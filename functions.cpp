&  ->  bit wise and

|  ->  bit wise or

^  ->  XOR operator

~  ->  Not operator (usually flips bits)


trunc(log10(num)) + 1 // tells the length of the number

s= s.substr(0, i) // copy a substring from a string
